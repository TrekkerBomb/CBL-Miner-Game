package testminergame;

//import testminergame.MapGeneration.Block;

/**
 * class for updating the blockmap.
 */
public class MapUpdate {
    /**
     * updates maps every frame.
     */
    public void mapUpdate(MapGeneration map) {


        for (int i = 0; i < map.getGridsizeX(); i++) {
            for (int j = 0; j < map.getGridsizeY(); j++) {
                // TODO add update
            }
        }
    }
}
